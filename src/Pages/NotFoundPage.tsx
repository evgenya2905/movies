import { Box, Typography } from '@mui/material';

export const NotFoundPage = () => {
  return (
    <Box sx={{ textAlign: 'center', pt: '3rem' }}>
      <Typography variant="h3">Not Found!</Typography>
    </Box>
  );
};
